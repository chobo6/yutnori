# 1v1 모드(팀당 1명, 말 4개) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 기존 2v2(팀당 2명, 인당 말 2개) 방식은 그대로 두고, 대기실에서 명시적으로 선택 가능한 1v1 모드(팀당 1명이 말 4개를 전부 조종)를 추가한다.

**Architecture:** `MatchState`에 `mode` 필드를 추가해 서버가 인원/캐릭터 개수/말 개수 조건을 모드별로 분기하고, 턴 순서(`buildTurnOrder`)와 승리 조건(`checkWinner`)의 하드코딩된 "2인"/"말 2개" 가정을 일반화해 두 모드를 같은 코드 경로로 처리한다. 클라이언트는 대기실에 모드 토글과 1v1 전용 캐릭터 선택 위젯을 추가한다.

**Tech Stack:** Node + TypeScript + Colyseus(서버, TDD via vitest), React + TypeScript(클라이언트, 테스트 프레임워크 없음 — `npm run build` + 실제 브라우저 확인으로 검증).

**Spec:** `docs/superpowers/specs/2026-08-22-1v1-mode-design.md`

## Global Constraints

- `MatchState.mode`는 `"2v2" | "1v1"` 두 값만 가지며 기본값은 `"2v2"`.
- 2v2: 캐릭터 정확히 서로 다른 2종. 1v1: 캐릭터 정확히 4종, **중복 허용**.
- 모드별 팀당 인원(`requiredPerTeam`): 2v2=2, 1v1=1. 모드별 인당 말 개수(`piecesPerPlayer`): 2v2=2, 1v1=4.
- `maxClients`는 4로 유지 — 방을 잠그거나(`lock()`) 여분 인원을 쫓아내는 로직을 추가하지 않는다.
- 모드 전환 시 이미 고른 팀/캐릭터/준비 상태를 자동으로 초기화하지 않는다 — 새 모드 조건에 안 맞으면 `maybeStartGame`이 조용히 보류하고 대기실 UI가 안내한다.
- `pickMode`/`pickTeam`/`pickCharacters`/`ready`는 모두 `phase === "waiting"`일 때만 처리하고, 누구나 언제든 보낼 수 있다(권한 제한 없음, 기존 관례).
- 서버 순수 게임 로직(`server/src/game/*`)은 TDD로 작성한다.
- 클라이언트 타입은 서버 스키마를 손으로 미러링하는 기존 관례를 유지한다(`client/src/game/matchTypes.ts`).
- 커밋 메시지는 한국어로, `feat:`/`fix:` 같은 프리픽스 없이 작성한다.
- REQUIREMENTS.md 갱신은 이 플랜의 태스크에 포함하지 않는다 — 전체 태스크 완료 후 컨트롤러가 직접 v0.5로 갱신한다(스펙 §8).

---

## File Structure

- `server/src/game/turns.ts` (수정): `buildTurnOrder` 시그니처 일반화, `checkWinner`의 매직넘버 제거.
- `server/src/rooms/MatchState.ts` (수정): `mode` 필드 추가.
- `server/src/rooms/MatchRoom.ts` (수정): `pickMode` 핸들러 추가, `pickCharacters` 핸들러 모드 분기, `maybeStartGame()` 전면 재작성.
- `client/src/game/matchTypes.ts` (수정): `MatchState.mode` 필드 추가.
- `client/src/components/WaitingRoom.tsx` (수정): 모드 토글 버튼, 1v1 전용 캐릭터 선택 위젯(드롭다운 4개), 동적 안내 문구.

---

### Task 1: 턴 순서/승리 조건 일반화 — `turns.ts`

**Files:**
- Modify: `server/src/game/turns.ts`
- Test: `server/src/game/turns.test.ts`

**Interfaces:**
- Produces: `export function buildTurnOrder(teamAIds: string[], teamBIds: string[]): string[];`(기존엔 `[string, string]` 튜플만 받았음) / `export function checkWinner(pieces: Piece[], ownerId: string): boolean;`(시그니처는 동일, 내부 판정만 말 개수에 무관하게 변경)

- [ ] **Step 1: 실패하는 테스트 작성**

`server/src/game/turns.test.ts`의 `describe("buildTurnOrder", ...)` 블록에 테스트를 추가한다(기존 테스트 바로 아래):

```ts
  it("팀당 1명씩(1v1)이면 [A0, B0] 순서다", () => {
    const order = buildTurnOrder(["a1"], ["b1"]);
    expect(order).toEqual(["a1", "b1"]);
  });
```

`describe("checkWinner", ...)` 블록 끝(마지막 `it` 뒤)에 테스트를 추가한다:

```ts
  it("1v1처럼 말 4개를 조종하는 경우, 4개 중 하나라도 안 끝나면 아직 승리 아님", () => {
    const pieces = [
      finishedPiece("p1", "alice"),
      finishedPiece("p2", "alice"),
      finishedPiece("p3", "alice"),
      unfinishedPiece("p4", "alice"),
    ];
    expect(checkWinner(pieces, "alice")).toBe(false);
  });

  it("말 4개가 전부 완주하면 승리한다", () => {
    const pieces = [
      finishedPiece("p1", "alice"),
      finishedPiece("p2", "alice"),
      finishedPiece("p3", "alice"),
      finishedPiece("p4", "alice"),
    ];
    expect(checkWinner(pieces, "alice")).toBe(true);
  });
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test --workspace server -- turns.test.ts`
Expected: FAIL — 새 `buildTurnOrder` 테스트는 타입 에러(1-길이 배열을 `[string,string]` 파라미터에 넣을 수 없음) 또는 런타임에서 `teamAIds[1]`이 `undefined`가 되어 `["a1", "b1", undefined, undefined]`가 나와 불일치. 새 `checkWinner` 테스트 2개는 현재 `ownPieces.length === 2` 체크 때문에 4개짜리 배열은 항상 `false`가 나와 "전부 완주" 테스트가 실패.

- [ ] **Step 3: 구현**

`server/src/game/turns.ts`의 `buildTurnOrder`와 `checkWinner`를 아래로 교체한다:

```ts
export function buildTurnOrder(teamAIds: string[], teamBIds: string[]): string[] {
  const order: string[] = [];
  for (let i = 0; i < teamAIds.length; i++) {
    order.push(teamAIds[i], teamBIds[i]);
  }
  return order;
}
```

```ts
export function checkWinner(pieces: Piece[], ownerId: string): boolean {
  const ownPieces = pieces.filter((p) => p.ownerId === ownerId);
  return ownPieces.length > 0 && ownPieces.every((p) => p.position.kind === "finished");
}
```

(`nextTurnIndex`는 변경하지 않는다 — 이미 `order: string[]`의 길이에만 의존하는 일반적인 구현이라 그대로 둔다.)

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test --workspace server -- turns.test.ts`
Expected: PASS (전체 통과, 신규 테스트 4개 포함)

- [ ] **Step 5: 커밋**

```bash
git add server/src/game/turns.ts server/src/game/turns.test.ts
git commit -m "buildTurnOrder를 임의 인원수로, checkWinner를 임의 말 개수로 일반화"
```

---

### Task 2: 모드 선택 + 캐릭터 검증 모드 분기 — `MatchState.ts` / `MatchRoom.ts`

**Files:**
- Modify: `server/src/rooms/MatchState.ts`
- Modify: `server/src/rooms/MatchRoom.ts`
- Test: `server/src/rooms/MatchRoom.test.ts`

**Interfaces:**
- Consumes: 없음(Task 1과 독립 — `buildTurnOrder`/`checkWinner`는 이 태스크에서 호출하지 않는다).
- Produces: `MatchState.mode: string`("2v2" | "1v1", 기본 "2v2") — Task 3이 이 필드를 읽어 `maybeStartGame()`을 분기한다.

**배경:** 이 태스크는 "모드를 고르고, 그 모드에 맞게 캐릭터 선택이 검증되는지"만 다룬다. 실제 게임 시작 조건(`maybeStartGame`)은 Task 3에서 다룬다 — 이 둘을 나눈 이유는 메시지 검증과 게임-시작 로직이 서로 다른 관심사이고, 각각 독립적으로 리뷰 가능해야 하기 때문이다.

- [ ] **Step 1: 실패하는 테스트 작성**

`server/src/rooms/MatchRoom.test.ts`의 "payload가 없거나 형식이 잘못된 메시지는..." 테스트 바로 뒤에 아래 테스트들을 추가한다:

```ts
  it("pickMode로 모드를 바꿀 수 있다", async () => {
    const room = await colyseus.createRoom<MatchState>("match", {});
    const client = await colyseus.connectTo(room);

    expect(room.state.mode).toBe("2v2");
    client.send("pickMode", { mode: "1v1" });
    await flush();
    expect(room.state.mode).toBe("1v1");
  });

  it("잘못된 mode 값은 무시된다", async () => {
    const room = await colyseus.createRoom<MatchState>("match", {});
    const client = await colyseus.connectTo(room);

    client.send("pickMode", { mode: "3v3" });
    await flush();
    expect(room.state.mode).toBe("2v2");
  });

  it("1v1 모드에서는 캐릭터 4종을 골라야 반영된다(2종은 무시)", async () => {
    const room = await colyseus.createRoom<MatchState>("match", {});
    const client = await colyseus.connectTo(room);

    client.send("pickMode", { mode: "1v1" });
    await flush();
    client.send("pickCharacters", { characters: ["교주", "성직"] });
    await flush();
    expect(room.state.players.get(client.sessionId)!.characters.length).toBe(0);

    client.send("pickCharacters", { characters: ["교주", "성직", "마담", "의사"] });
    await flush();
    expect(Array.from(room.state.players.get(client.sessionId)!.characters)).toEqual([
      "교주",
      "성직",
      "마담",
      "의사",
    ]);
  });

  it("1v1 모드에서는 캐릭터 중복이 허용된다", async () => {
    const room = await colyseus.createRoom<MatchState>("match", {});
    const client = await colyseus.connectTo(room);

    client.send("pickMode", { mode: "1v1" });
    await flush();
    client.send("pickCharacters", { characters: ["의사", "의사", "마담", "마담"] });
    await flush();
    expect(Array.from(room.state.players.get(client.sessionId)!.characters)).toEqual([
      "의사",
      "의사",
      "마담",
      "마담",
    ]);
  });

  it("2v2 모드(기본값)에서는 캐릭터 중복이 거부된다", async () => {
    const room = await colyseus.createRoom<MatchState>("match", {});
    const client = await colyseus.connectTo(room);

    client.send("pickCharacters", { characters: ["교주", "교주"] });
    await flush();
    expect(room.state.players.get(client.sessionId)!.characters.length).toBe(0);
  });
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test --workspace server -- MatchRoom.test.ts`
Expected: FAIL — `room.state.mode`가 `undefined`이고 `pickMode` 메시지를 처리하는 핸들러가 없어 아무 반응이 없음. 중복 캐릭터 거부 테스트는 현재 검증 로직에 중복 체크가 없어 실패(캐릭터가 반영되어 버림).

- [ ] **Step 3: 구현**

`server/src/rooms/MatchState.ts`의 `MatchState` 클래스에 필드를 추가한다(`phase` 필드 바로 아래):

```ts
export class MatchState extends Schema {
  @type("string") phase: string = "waiting"; // "waiting" | "playing" | "finished"
  @type("string") mode: string = "2v2"; // "2v2" | "1v1"
  @type({ map: PlayerState }) players = new MapSchema<PlayerState>();
```

`server/src/rooms/MatchRoom.ts`의 `pickTeam` 핸들러 바로 뒤(그리고 `pickCharacters` 핸들러 앞)에 새 핸들러를 추가한다:

```ts
    this.onMessage("pickMode", (client, message: { mode: "2v2" | "1v1" } | undefined) => {
      if (this.state.phase !== "waiting") return;
      if (message?.mode !== "2v2" && message?.mode !== "1v1") return;
      this.state.mode = message.mode;
    });
```

기존 `pickCharacters` 핸들러를 아래로 교체한다:

```ts
    this.onMessage("pickCharacters", (client, message: { characters: string[] } | undefined) => {
      if (this.state.phase !== "waiting") return;
      if (!Array.isArray(message?.characters)) return;
      const requiredCount = this.state.mode === "1v1" ? 4 : 2;
      if (message.characters.length !== requiredCount) return;
      if (!message.characters.every((c) => VALID_CHARACTERS.has(c))) return;
      if (this.state.mode !== "1v1" && new Set(message.characters).size !== message.characters.length) return;
      const player = this.state.players.get(client.sessionId);
      if (!player) return;
      player.characters.clear();
      for (const c of message.characters) player.characters.push(c);
    });
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test --workspace server -- MatchRoom.test.ts`
Expected: PASS (전체 통과 — 기존 `setupFourPlayers` 기반 테스트들은 모드를 건드리지 않아 기본값 `"2v2"`로 계속 정상 동작해야 한다)

- [ ] **Step 5: 커밋**

```bash
git add server/src/rooms/MatchState.ts server/src/rooms/MatchRoom.ts server/src/rooms/MatchRoom.test.ts
git commit -m "대기실에 모드(2v2/1v1) 선택 추가, 캐릭터 선택 검증을 모드별로 분기"
```

---

### Task 3: 게임 시작 조건 & 말 생성 — `maybeStartGame()` 재작성

**Files:**
- Modify: `server/src/rooms/MatchRoom.ts`
- Test: `server/src/rooms/MatchRoom.test.ts`

**Interfaces:**
- Consumes: Task 1의 `buildTurnOrder(teamAIds: string[], teamBIds: string[]): string[]`(일반화된 시그니처). Task 2의 `MatchState.mode`.
- Produces: 없음(이 태스크가 이 플랜의 마지막 서버 로직 변경) — Task 4(클라이언트)는 이 태스크의 서버 동작 결과(상태 필드)만 소비하며 별도 함수 시그니처를 공유하지 않는다.

- [ ] **Step 1: 실패하는 테스트 작성**

`server/src/rooms/MatchRoom.test.ts`의 첫 번째 `it` 블록("4명이 팀/캐릭터를 정하고 준비하면 게임이 시작된다") 바로 뒤에 아래 테스트들을 추가한다:

```ts
  it("1v1 모드에서는 2명(팀당 1명)이 캐릭터 4종씩 고르고 준비하면 게임이 시작되고 말이 8개(4개씩) 생긴다", async () => {
    const room = await colyseus.createRoom<MatchState>("match", {});
    const clientA = await colyseus.connectTo(room);
    const clientB = await colyseus.connectTo(room);

    clientA.send("pickMode", { mode: "1v1" });
    await flush();

    clientA.send("pickTeam", { team: "A" });
    clientA.send("pickCharacters", { characters: ["교주", "성직", "마담", "의사"] });
    clientB.send("pickTeam", { team: "B" });
    clientB.send("pickCharacters", { characters: ["의사", "의사", "마담", "마담"] });
    await flush();

    clientA.send("ready", {});
    clientB.send("ready", {});
    await flush();

    expect(room.state.phase).toBe("playing");
    expect(room.state.pieces.length).toBe(8);
    expect(room.state.turnOrder.length).toBe(2);
    expect(room.state.pieces.filter((p) => p.ownerSessionId === clientA.sessionId).length).toBe(4);
    expect(room.state.pieces.filter((p) => p.ownerSessionId === clientB.sessionId).length).toBe(4);
  });

  it("1v1 모드에서 한 팀에 2명이 들어와 인원이 안 맞으면 게임이 시작되지 않는다", async () => {
    const room = await colyseus.createRoom<MatchState>("match", {});
    const clients = await Promise.all([colyseus.connectTo(room), colyseus.connectTo(room), colyseus.connectTo(room)]);

    clients[0].send("pickMode", { mode: "1v1" });
    await flush();

    clients[0].send("pickTeam", { team: "A" });
    clients[0].send("pickCharacters", { characters: ["교주", "성직", "마담", "의사"] });
    clients[1].send("pickTeam", { team: "A" });
    clients[1].send("pickCharacters", { characters: ["교주", "성직", "마담", "의사"] });
    clients[2].send("pickTeam", { team: "B" });
    clients[2].send("pickCharacters", { characters: ["교주", "성직", "마담", "의사"] });
    await flush();
    for (const c of clients) c.send("ready", {});
    await flush();

    expect(room.state.phase).toBe("waiting");
  });
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test --workspace server -- MatchRoom.test.ts`
Expected: FAIL — `maybeStartGame()`이 여전히 `players.size !== 4`로 시작 조건을 걸어놔서, 2명뿐인 1v1 시나리오는 절대 시작되지 않는다(`room.state.phase`가 계속 `"waiting"`으로 남아 첫 번째 새 테스트가 실패).

- [ ] **Step 3: 구현**

`server/src/rooms/MatchRoom.ts`의 `maybeStartGame()` 메서드 전체를 아래로 교체한다:

```ts
  private maybeStartGame() {
    const requiredPerTeam = this.state.mode === "1v1" ? 1 : 2;
    const requiredCharacters = this.state.mode === "1v1" ? 4 : 2;
    const piecesPerPlayer = this.state.mode === "1v1" ? 4 : 2;

    if (this.state.players.size !== requiredPerTeam * 2) return;
    const allPlayers = Array.from(this.state.players.values());
    if (!allPlayers.every((p) => p.ready && p.characters.length === requiredCharacters)) return;

    const teamA = allPlayers.filter((p) => p.team === "A").map((p) => p.sessionId);
    const teamB = allPlayers.filter((p) => p.team === "B").map((p) => p.sessionId);
    if (teamA.length !== requiredPerTeam || teamB.length !== requiredPerTeam) return;

    const order = buildTurnOrder(teamA, teamB);
    this.state.turnOrder.clear();
    for (const id of order) this.state.turnOrder.push(id);
    this.state.currentTurnIndex = 0;

    this.state.pieces.clear();
    for (const sessionId of [...teamA, ...teamB]) {
      const owner = this.state.players.get(sessionId)!;
      for (let i = 0; i < piecesPerPlayer; i++) {
        const piece = new PieceSchema();
        piece.id = `${sessionId}-${i}`;
        piece.ownerSessionId = sessionId;
        piece.character = owner.characters[i];
        piece.positionKind = "start";
        piece.positionIndex = -1;
        piece.previousPositionKind = "start";
        piece.previousPositionIndex = -1;
        this.state.pieces.push(piece);
      }
    }

    this.state.phase = "playing";
    this.armThrowTimeout(this.state.turnOrder[this.state.currentTurnIndex]);
  }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test --workspace server -- MatchRoom.test.ts`
Expected: PASS (전체 통과)

Run: `npm test --workspace server`
Expected: 서버 전체 테스트 스위트 통과 — `MatchRoom.fullGame.test.ts`, `MatchRoom.abilities.test.ts`는 모드를 지정하지 않아 기본값 `"2v2"`로 기존과 동일하게 동작해야 한다(회귀 없음).

Run: `npm run build --workspace server`
Expected: 타입 에러 없음

- [ ] **Step 5: 커밋**

```bash
git add server/src/rooms/MatchRoom.ts server/src/rooms/MatchRoom.test.ts
git commit -m "maybeStartGame이 모드(2v2/1v1)에 맞는 인원/캐릭터/말 개수로 게임을 시작하도록 재작성"
```

---

### Task 4: 대기실 UI — 모드 토글 + 1v1 캐릭터 선택

**Files:**
- Modify: `client/src/game/matchTypes.ts`
- Modify: `client/src/components/WaitingRoom.tsx`

**Interfaces:**
- Consumes: Task 2/3에서 서버가 브로드캐스트하는 `MatchState.mode`("2v2" | "1v1") — 서버 스키마를 손으로 미러링하는 기존 관례.

**배경:** client에는 자동화 테스트 프레임워크가 없다(CLAUDE.md). `npm run build`(타입체크)로 1차 검증하고, 반드시 실제 브라우저에서 2개 탭으로 1v1 모드를 끝까지(입장→모드 선택→팀/캐릭터 선택→준비→게임 시작) 확인한다 — `docs/TROUBLESHOOTING.md` #9/#10이 코드만 보고 정상이라 판단했다가 실제로는 깨져 있던 사례라, 이 프로젝트는 클라이언트 변경마다 이 확인을 필수로 요구한다.

- [ ] **Step 1: 클라이언트 타입에 필드 추가**

`client/src/game/matchTypes.ts`의 `MatchState` 인터페이스를 아래로 교체:

```ts
export interface MatchState {
  phase: "waiting" | "playing" | "finished";
  mode: "2v2" | "1v1";
  players: Map<string, PlayerState>;
  pieces: PieceState[];
  turnOrder: string[];
  currentTurnIndex: number;
  gaugePhase: "idle" | "charging" | "resolved";
  throwStartAt: number;
  lastThrowResult: string;
  turnDeadlineAt: number;
  winnerSessionId: string;
}
```

- [ ] **Step 2: WaitingRoom.tsx 교체**

`client/src/components/WaitingRoom.tsx` 파일 전체를 아래로 교체한다:

```tsx
import { useState } from "react";
import type { Room } from "colyseus.js";
import { CHARACTERS, type CharacterId, type MatchState } from "../game/matchTypes";
import { playerLabel } from "../game/playerLabel";
import styles from "./WaitingRoom.module.css";

export function WaitingRoom({ room }: { room: Room<MatchState> }) {
  const me = room.state.players.get(room.sessionId);
  const [pendingCharacters, setPendingCharacters] = useState<CharacterId[]>(
    (me?.characters ?? []) as CharacterId[],
  );
  const [slotCharacters, setSlotCharacters] = useState<CharacterId[]>(() => {
    const existing = (me?.characters ?? []) as CharacterId[];
    return existing.length === 4 ? existing : [CHARACTERS[0], CHARACTERS[0], CHARACTERS[0], CHARACTERS[0]];
  });

  const mode = room.state.mode;
  const requiredPerTeam = mode === "1v1" ? 1 : 2;
  const requiredCharacters = mode === "1v1" ? 4 : 2;
  const totalRequired = requiredPerTeam * 2;

  function pickMode(nextMode: "2v2" | "1v1") {
    room.send("pickMode", { mode: nextMode });
  }

  function pickTeam(team: "A" | "B") {
    room.send("pickTeam", { team });
  }

  function toggleCharacter(character: CharacterId) {
    setPendingCharacters((prev) => {
      let next: CharacterId[];
      if (prev.includes(character)) {
        // 이미 2개 골라 서버에 보낸 상태에서의 해제는 no-op.
        // 해제만 하면 서버로 보내는 메시지가 없어 서버는 옛 2개를 그대로 들고 있고,
        // 그 사이 "준비 완료"를 누르면 화면에 보이는 것과 다른 캐릭터로 게임이 시작된다.
        // 마음을 바꾸려면 아래 "3번째를 골라 가장 오래된 것을 밀어내는" 경로(정상 동기화됨)를 쓰면 된다.
        if (prev.length >= 2) return prev;
        next = prev.filter((c) => c !== character);
      } else if (prev.length >= 2) {
        next = [prev[1], character]; // 가장 오래된 선택을 밀어내고 새로 추가
      } else {
        next = [...prev, character];
      }
      if (next.length === 2) {
        room.send("pickCharacters", { characters: next });
      }
      return next;
    });
  }

  function updateSlot(index: number, character: CharacterId) {
    setSlotCharacters((prev) => {
      const next = [...prev];
      next[index] = character;
      room.send("pickCharacters", { characters: next });
      return next;
    });
  }

  function toggleReady() {
    room.send("ready", {});
  }

  const players = Array.from(room.state.players.values());

  // 서버 maybeStartGame은 팀이 모드에 맞는 인원으로 나뉘고 각자 캐릭터를 다 골라야만 시작한다.
  // 조건이 안 맞으면 아무 일도 없이 조용히 넘어가므로, 왜 안 시작하는지 여기서 알려준다.
  const teamACount = players.filter((p) => p.team === "A").length;
  const teamBCount = players.filter((p) => p.team === "B").length;
  const teamSplitOk = teamACount === requiredPerTeam && teamBCount === requiredPerTeam;
  const charactersMissing = players.filter((p) => p.characters.length !== requiredCharacters).length;

  return (
    <div className={styles.wrap}>
      <h2>대기실</h2>

      <section>
        <h3>모드 선택</h3>
        <button
          type="button"
          className={mode === "2v2" ? styles.selected : undefined}
          onClick={() => pickMode("2v2")}
        >
          2v2
        </button>
        <button
          type="button"
          className={mode === "1v1" ? styles.selected : undefined}
          onClick={() => pickMode("1v1")}
        >
          1v1
        </button>
      </section>

      <section>
        <h3>팀 선택</h3>
        <button
          type="button"
          className={me?.team === "A" ? styles.selected : undefined}
          onClick={() => pickTeam("A")}
        >
          A팀
        </button>
        <button
          type="button"
          className={me?.team === "B" ? styles.selected : undefined}
          onClick={() => pickTeam("B")}
        >
          B팀
        </button>
      </section>

      {mode === "2v2" ? (
        <section>
          <h3>캐릭터 선택 (2종)</h3>
          {CHARACTERS.map((character) => (
            <button
              key={character}
              type="button"
              className={pendingCharacters.includes(character) ? styles.selected : undefined}
              onClick={() => toggleCharacter(character)}
            >
              {character}
            </button>
          ))}
        </section>
      ) : (
        <section>
          <h3>캐릭터 선택 (말 4개, 중복 가능)</h3>
          {slotCharacters.map((character, index) => (
            <label key={index}>
              말 {index + 1}:{" "}
              <select value={character} onChange={(e) => updateSlot(index, e.target.value as CharacterId)}>
                {CHARACTERS.map((c) => (
                  <option key={c} value={c}>
                    {c}
                  </option>
                ))}
              </select>
            </label>
          ))}
        </section>
      )}

      <section>
        <button type="button" onClick={toggleReady}>
          {me?.ready ? "준비 취소" : "준비 완료"}
        </button>
      </section>

      <section>
        <h3>
          참가자 ({players.length}/{totalRequired})
        </h3>
        {players.length === totalRequired && !teamSplitOk && (
          <p>
            A팀 {requiredPerTeam}명 / B팀 {requiredPerTeam}명이 되어야 게임이 시작됩니다 (현재 A팀 {teamACount}명,
            B팀 {teamBCount}명)
          </p>
        )}
        {players.length === totalRequired && teamSplitOk && charactersMissing > 0 && (
          <p>
            모두 캐릭터를 {requiredCharacters}종씩 골라야 게임이 시작됩니다 (아직 {charactersMissing}명 미완료)
          </p>
        )}
        <ul>
          {players.map((player) => (
            <li key={player.sessionId}>
              {playerLabel(player.sessionId, room)} — 팀: {player.team || "미정"}, 캐릭터:{" "}
              {player.characters.length > 0 ? player.characters.join(", ") : "미정"}, 준비:{" "}
              {player.ready ? "완료" : "대기중"}
            </li>
          ))}
        </ul>
      </section>
    </div>
  );
}
```

- [ ] **Step 3: 타입체크**

Run: `npm run build --workspace client`
Expected: 타입 에러 없음(`tsc -b && vite build` 성공)

- [ ] **Step 4: 실제 브라우저로 1v1 전체 플로우 확인**

`npm run dev`로 서버+클라이언트를 띄운 뒤(또는 Playwright의 `browser_navigate` 등으로), 브라우저 탭 2개로 `http://localhost:5173`에 접속해 아래를 확인한다:

1. 탭 1에서 "1v1" 버튼 클릭 → 탭 2에서도 모드가 "1v1"로 동기화되어 보이는지 확인(캐릭터 선택 UI가 드롭다운 4개로 바뀌는지).
2. 탭 1: A팀 선택, 말 1~4 드롭다운에서 캐릭터를 각각 지정(하나는 일부러 같은 캐릭터를 2개 선택해 중복이 허용되는지 확인). 탭 2: B팀 선택, 캐릭터 4개 지정.
3. 두 탭 모두 "준비 완료" 클릭 → 게임이 시작되는지(`phase: playing`) 확인.
4. 게임 화면에서 자기 턴이 된 쪽이 말 4개에 대한 이동 버튼을 볼 수 있는지 확인(`TurnPanel`이 별도 수정 없이 4개 말을 그려주는지 — 스펙 §7에서 이미 예상한 대로 동작해야 함).

문제가 있으면(예: 모드 동기화 지연, 드롭다운 값이 서버에 반영 안 됨 등) 원인을 찾아 수정하고 다시 확인한다.

- [ ] **Step 5: 커밋**

```bash
git add client/src/game/matchTypes.ts client/src/components/WaitingRoom.tsx
git commit -m "대기실에 1v1 모드 토글과 말 4개 캐릭터 선택(드롭다운) 추가"
```

---

## Self-Review 결과

- **스펙 커버리지:** §1(모드 선택/방 상태)는 Task 2, §2(캐릭터 선택 개수/중복)는 Task 2, §3(대기실 UI)는 Task 4, §4(턴 순서 일반화)는 Task 1, §5(말 생성)는 Task 3, §6(승리 조건)은 Task 1, §7(데이터 모델 변경 요약)은 Task 1~4 전체에 매핑, §8(REQUIREMENTS.md 갱신)은 태스크가 아니라 Global Constraints에 명시한 대로 컨트롤러가 태스크 완료 후 직접 처리. §9(범위 제외)는 어떤 태스크에도 해당 기능을 넣지 않음으로써 자연히 지켜짐. 누락 없음.
- **플레이스홀더 스캔:** "TBD"/"TODO"/"적절히 처리" 패턴 없음. 모든 스텝에 실행 가능한 실제 코드 포함.
- **타입 일관성 확인:** `buildTurnOrder(teamAIds: string[], teamBIds: string[])`(Task 1) → Task 3의 `maybeStartGame()`에서 `buildTurnOrder(teamA, teamB)`로 그대로 호출(둘 다 `string[]`, 타입 일치). `MatchState.mode: string`(Task 2, 서버) ↔ `MatchState.mode: "2v2" | "1v1"`(Task 4, 클라이언트 타입) — 값 자체는 항상 두 리터럴 중 하나만 서버가 넣으므로 클라이언트 쪽 좁은 유니언 타입과 실제 런타임 값이 일치. `requiredPerTeam`/`requiredCharacters`/`piecesPerPlayer` 계산식이 Task 2(캐릭터 개수만), Task 3(인원+말 개수), Task 4(클라이언트 안내 문구)에서 각각 독립적으로 `mode === "1v1" ? X : Y` 형태로 반복되는데, 세 곳 모두 정확히 같은 값(1v1→{인원1, 캐릭터4, 말4}, 2v2→{인원2, 캐릭터2, 말2})을 쓰고 있음을 확인 — 이름은 다르지만 로직 불일치 없음.

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-08-22-1v1-mode.md`. 실행 방식을 선택해주세요:

1. **Subagent-Driven (권장)** — 태스크마다 새 서브에이전트를 붙여 구현시키고, 태스크 사이사이 리뷰하며 빠르게 진행합니다.
2. **Inline Execution** — 이 세션에서 executing-plans로 배치 실행하고, 체크포인트마다 검토합니다.

어느 쪽으로 진행할까요?
